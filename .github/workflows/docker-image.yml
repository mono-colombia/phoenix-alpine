name: Build & publish Docker image

on:
  push:
    branches: [master, sre/gpg-secmem]

concurrency:
  group: docker-image-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: us-east-1
  GHCR_IMAGE: ghcr.io/mono-colombia/phoenix-alpine
  DOCKERHUB_IMAGE: monocolombia/phoenix-alpine
  ECR_PUBLIC_IMAGE: public.ecr.aws/monocolombia/phoenix-alpine

jobs:
  version:
    name: version
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Read tag from VERSION file
        id: tag
        run: echo "tag=$(cat VERSION)" >> "$GITHUB_OUTPUT"

  build:
    name: build_${{ matrix.arch }}
    needs: version
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - { arch: amd64, runner: ubuntu-latest,    platform: linux/amd64 }
          - { arch: arm64, runner: ubuntu-24.04-arm, platform: linux/arm64 }
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (${{ matrix.platform }})
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ matrix.platform }}
          pull: true
          load: true
          tags: ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }}-${{ matrix.arch }}
          cache-from: type=gha,scope=${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.arch }}

      - name: Verify Elixir
        run: docker run --rm ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }}-${{ matrix.arch }} sh -c "elixir --version"

      - name: Push staging image to GHCR
        run: docker push ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }}-${{ matrix.arch }}

  push-multi-arch:
    name: push-multi-arch
    needs: [build, version]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_OIDC }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: "github-actions-session"

      - name: Configure AWS Credentials (Mono Containers Custom)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_ROLE_MONO_CONTAINERS_CUSTOM }}
          aws-region: ${{ env.AWS_REGION }}
          role-external-id: "mono-containers-custom"
          role-session-name: "github-actions-mono-containers-custom"
          role-skip-session-tagging: true
          role-chaining: true

      - name: Login to AWS ECR Public
        uses: aws-actions/amazon-ecr-login@v2
        with:
          registry-type: public

      - name: Create multi-arch manifest on GHCR
        run: |
          docker buildx imagetools create \
            -t ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }} \
            ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }}-amd64 \
            ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }}-arm64

      - name: Publish multi-arch image to Docker Hub & ECR Public
        run: |
          docker buildx imagetools create -t ${{ env.DOCKERHUB_IMAGE }}:${{ needs.version.outputs.tag }} ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }}
          docker buildx imagetools create -t ${{ env.ECR_PUBLIC_IMAGE }}:${{ needs.version.outputs.tag }} ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }}

      - name: Inspect manifests
        if: always()
        run: |
          for img in \
            ${{ env.GHCR_IMAGE }}:${{ needs.version.outputs.tag }} \
            ${{ env.DOCKERHUB_IMAGE }}:${{ needs.version.outputs.tag }} \
            ${{ env.ECR_PUBLIC_IMAGE }}:${{ needs.version.outputs.tag }}; do
            echo "---- $img ----"
            docker buildx imagetools inspect "$img" || true
          done
